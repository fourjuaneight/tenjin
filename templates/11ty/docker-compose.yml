version: '3'
services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /app
    command:
      - 'npm'
      - 'start'
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "8080:8080"